<%= form_for(@article) do |f| %>
		
		<% @article.errors.full_messages.each do |message| %>
			<div class="be-red white">
				<%= message %>
			</div>
		<%end%>
		<div class= "field">
			<%= f.text_field :title, placeholder: "Título" , class:"form-control"%>
		</div>

		<div class= "field">
			<%= f.text_area :down, placeholder: "Bajada", style: "height:100px;", class:"form-control"%>
		</div>

		<div class= "field">
			<%= f.text_area :body, placeholder: "Cuerpo", style: "height:200px;", class:"form-control"%>
		</div>
		<div>
			<%= f.submit "Guardar", class:"btn"%>
		</div>

<%end%>